[build]
builder = "DOCKERFILE"
dockerfilePath = "apps/parser-service/Dockerfile"
buildContext = "apps/parser-service"

[deploy]
startCommand = "uvicorn app.main:app --host 0.0.0.0 --port $PORT"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
